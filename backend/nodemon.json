{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts", "dist"],
  "exec": "ts-node --project tsconfig.json --transpileOnly src/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
